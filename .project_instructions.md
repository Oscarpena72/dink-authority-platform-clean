## PWA (Progressive Web App)
- **manifest.json**: Already existed in `public/` with icons 192x192 and 512x512, standalone display, brand-purple theme
- **Service Worker** (`public/sw.js`): Network-first for pages (with offline fallback), cache-first for static assets, skips API routes
- **Offline page** (`public/offline.html`): Branded offline fallback with retry button
- **SW Registration** (`_components/pwa-register.tsx`): Registers SW on mount
- **Install Prompt** (`_components/pwa-install-prompt.tsx`): Smart banner for Android/desktop (uses beforeinstallprompt) + iOS guide modal (step-by-step Safari instructions). Dismissible for 24h, hidden if already standalone.
- Both PWA components loaded in root `layout.tsx`

## Phase 3A — Subscriber Capture (Email + Phone)
- **New model**: `Subscriber` (id, email unique, phoneNumber optional, source, subscribedAt) — separate from existing `NewsletterSubscriber`
- **API**: `POST /api/subscribers` (public, upsert by email) + `GET /api/subscribers` (admin, supports `?format=csv` export)
- **Inline subscribe block**: Inserted at ~37% of article content (both desktop and mobile) via `SubscribeForm` component with source="article"
- **Footer subscribe form**: Added as 4th column in footer grid via `SubscribeForm` component with source="footer"
- **Admin page**: `/admin/subscribers` with table, search, refresh, CSV export button
- **Admin nav**: "Subscribers" item added with Users icon between Newsletter and Settings

## Phase 3B — SEO / Discover Engine
- **robots.ts**: Dynamic, allows /articles, /magazine, /about, /contact; disallows /admin, /api, /login; references sitemap
- **sitemap.ts**: Dynamic, includes all published articles (priority 0.8), magazine editions (priority 0.7), static pages (/, /articles, /magazine, /about, /contact)
- **Article page metadata** (`articles/[slug]/page.tsx`):
  - Canonical URL via `alternates.canonical`
  - `robots: { index: true, follow: true, 'max-image-preview': 'large', 'max-snippet': -1, 'max-video-preview': -1 }`
  - Complete OpenGraph: type article, publishedTime, modifiedTime, authors, section, locale
  - Twitter Card: summary_large_image with site handle
- **JSON-LD Structured Data**: Two schemas injected in article pages:
  - `NewsArticle` with headline, image, datePublished, dateModified, author, publisher (with logo), description, mainEntityOfPage
  - `BreadcrumbList` with Home → Articles → Article Title
- **Visible breadcrumbs**: Replaced "Back to Articles" link with `Home / Articles / Title` breadcrumb trail in article-detail-client.tsx
- **Global max-image-preview**: Added `<meta name="robots" content="max-image-preview:large" />` in root layout.tsx head

## Community Correspondent Module ("El Reportero Eres Tú")
- **Database model**: `CommunityCorrespondent` (firstName, lastName, city, state, address, zipCode, phone, email, facebookUrl, instagramUrl, tiktokUrl, message, status, createdAt)
- **Status values**: `new`, `reviewed`, `approved`, `rejected`
- **API routes**: `/api/community` (POST public registration + GET admin with CSV export), `/api/community/[id]` (PUT status + DELETE)
- **Email notification**: Sends to `contact@dinkauthoritymagazine.com` via Abacus notification API (NOTIF_ID_COMMUNITY_CORRESPONDENT_REGISTRATION)
- **Public page**: `/community` — hero with editorial text (EN/ES/PT), benefits cards, registration form, social share buttons, SEO metadata + JSON-LD
- **Homepage CTA**: Injected after 5th news item in LatestNews grid (community-cta.tsx) — editorial-style block with gradient, taglines, REGÍSTRATE button
- **Admin CMS**: `/admin/community` — expandable list, status management (dropdown), delete, CSV export, detail view with social links
- **Admin sidebar**: "Community" with Megaphone icon added after Juniors
- **i18n keys**: 40+ `community.*` keys (hero, editorial, benefits, form, share, confirmation, CTA)
- **Hero image**: `/public/images/community-hero.jpg` (AI-generated, editorial style with brand colors)

## Shop / Store Section
- **Product model**: `Product` (name, slug unique, category, price, shortDescription, fullDescription, images String[], stripePaymentLink, buttonLabel, isActive, isFeatured, inventoryStatus)
- **inventoryStatus values**: `in_stock`, `out_of_stock`, `coming_soon`
- **No cart/checkout**: Uses Stripe Payment Links exclusively. "Buy Now" button opens external Stripe link.
- **API routes**: `/api/products` (GET list, POST create), `/api/products/[id]` (GET, PUT, DELETE)
- **Admin**: `/admin/products` (list), `/admin/products/new` (create), `/admin/products/[id]/edit` (edit). Added "Products" with ShoppingBag icon in admin sidebar nav.
- **Public pages**: `/shop` (product grid catalog with category filter), `/shop/[slug]` (product detail with image gallery, breadcrumbs, related products)
- **Header nav**: "Shop" link added at end of NAV_ITEMS → `/shop`
- **i18n keys**: `nav.shop`, `shop.title`, `shop.subtitle`, `shop.viewProduct`, `shop.buyNow`, `shop.description`, `shop.relatedProducts`
- **Demo products**: 5 seeded — Hoodie ($59.99), Cap ($29.99), T-Shirt ($34.99), Tumbler ($24.99), Mug ($19.99). Images in `/public/images/products/`
- **Product images**: Up to 5 URL-based images per product. First image is "Main". Added via URL input in admin form.

## Phase 3C — Google News / Discover Optimization
- **Google News Sitemap** (`/news-sitemap.xml`): Dynamic route at `app/news-sitemap.xml/route.ts`, returns XML with `<news:news>` tags for articles published in last 48 hours. Cached 5 min.
- **robots.ts**: Updated sitemap field to array: `[sitemap.xml, news-sitemap.xml]`
- **Publisher Identity**: Organization + WebSite JSON-LD schemas added to root layout.tsx `<head>`. Includes name, url, logo (ImageObject with dimensions), sameAs (ready for social links), description, and SearchAction.
- **Article Author Metadata**: Enhanced Person schema in article JSON-LD with `url` field. Fallback to "Dink Authority Editorial Team" when no author. OG authors also uses same fallback.
- **Discover Image Optimization**: Article JSON-LD images now use ImageObject with explicit `width: 1200, height: 630`. OG images already had width/height.
- **Freshness Signals**: Prisma `@updatedAt` on Article model auto-updates `dateModified` on every edit. Already reflected in JSON-LD and OG modifiedTime.
- **Google News Friendly URLs**: Confirmed clean `/articles/[slug]` structure, no params/IDs.
- **Discover Image Ratio**: Hero uses 16:9, OG images 1200x630 (~16:9). No layout changes needed.
- **isAccessibleForFree**: Added to NewsArticle JSON-LD schema.

## Tips Section (Pro Pickleball Tips)
- **Database models**: `TipAuthor` (id, name, slug unique, photoUrl, bio, timestamps) + `Tip` (id, title, slug unique, excerpt, featuredImage, authorId→TipAuthor, publishDate, category, content, galleryImages JSON string "[]", banner1/2/3 Image+Link, youtubeUrl, videoCtaText/Link, status, language, metaTitle, metaDescription, timestamps)
- **Categories**: technique, strategy, fitness, mental-game, equipment
- **API routes**: `/api/tips` (GET/POST), `/api/tips/[id]` (GET/PUT/DELETE), `/api/tip-authors` (GET/POST), `/api/tip-authors/[id]` (GET/PUT/DELETE)
- **Public pages**: `/tips` (listing with category filter, premium grid), `/tips/[slug]` (detail with hero, author header+photo+bio, content with 3 interleaved banner ad slots, YouTube embed, image gallery lightbox max 5, share buttons, related tips, JSON-LD structured data, full SEO metadata/OG)
- **Share buttons**: Facebook, X/Twitter, LinkedIn, WhatsApp, Email, Instagram (copy-link fallback), Copy Link
- **Admin CMS**: `/admin/tips` (list), `/admin/tips/new` (create), `/admin/tips/[id]/edit` (edit), `/admin/tips/authors` (manage authors inline)
- **Admin sidebar**: Added "Tips" (Lightbulb icon) and "Tip Authors" (UserCheck icon) after Articles
- **Header nav**: "Tips" link added to NAV_ITEMS → `/tips` (after Pro Players, before Enthusiasts)
- **i18n keys**: tips.title, tips.subtitle, tips.readTip, tips.by, tips.watchMore, tips.shareTitle, tips.copyLink, tips.copied, tips.gallery, tips.relatedTips
- **Seed data**: 4 demo tips (Kitchen Dink by Domenika Turkovic, Doubles Strategy by Lorena Duknic, Serve Technique by Dalia Garza, Footwork by Dink Authority Team) with real images, YouTube embeds, gallery images
- **4 TipAuthors seeded**: Domenika Turkovic, Lorena Duknic, Dalia Garza, Dink Authority Team (with real photos and bios)

## Universal Video Module
- **Component**: `components/universal-video-module.tsx` — single reusable component that auto-detects platform from URL
- **Supported platforms**: YouTube, Instagram, TikTok, Facebook
- **YouTube rendering**: 16:9 horizontal, click-to-play thumbnail → inline iframe embed
- **Instagram/TikTok/Facebook rendering**: 9:16 vertical editorial card with poster image, gradient border, play button overlay, platform branding, "Ver en [Platform]" CTA that opens in new tab (Instagram blocks iframe embeds)
- **Exported utilities**: `detectPlatform(url)` returns `'youtube'|'instagram'|'tiktok'|'facebook'|null`, `VideoPlatform` type
- **Database field**: `videoUrl String?` added to Tip, Junior, and Article models (unified field replacing old `youtubeUrl` and `instagramVideoUrl`)
- **Backward compatibility**: Old fields (`youtubeUrl` on Tip, `instagramVideoUrl` on Junior) kept in schema. Frontend falls back: `videoUrl ?? youtubeUrl` for Tips, `videoUrl ?? instagramVideoUrl` for Juniors
- **CMS admin forms**: All three (Tips, Juniors, Articles) have universal "Video URL" input accepting YouTube, Instagram, TikTok, or Facebook URLs
- **API routes**: All six endpoints (create+update for Tips, Juniors, Articles) handle `videoUrl`
- **i18n keys**: `video.sectionTitle`, `video.play`, `video.watchOn`
- **Data migrated**: Existing `youtubeUrl` values copied to `videoUrl` for Tips; existing `instagramVideoUrl` values copied to `videoUrl` for Juniors
- **Video Poster Image**: `videoPosterImage String?` field on all three models. CMS shows "Video Poster Image" input (optional). If empty, frontend falls back to featured/main image. Logic: `videoPosterImage || featuredImage`
- **Pro Players**: NOT a separate model/route — just articles with `category: 'pro-players'`. Access via `/articles?category=pro-players`. Video module works automatically since Articles already have UniversalVideoModule.

## Dink Authority World (Country Editions)
- **Database model**: `Country` with fields: name, slug (unique), code, flagEmoji, isActive, sortOrder, SEO (metaTitle, metaDescription, ogImage), Magazine (magazineCoverImage, magazineTitle, magazineLink, magazinePdfUrl), Content boxes (newsBox, proPlayersBox, enthusiastsBox, juniorsBox, tipsBox — JSON string arrays of paths like `["/articles/slug", "/tips/slug"]`), Banners (topBanner/midBanner/bottomBanner/stickyBanner with Image+Link), socialMedia (JSON with instagram/facebook/tiktok/youtube/twitter), timestamps
- **Seeded countries**: Colombia (CO 🇨🇴), Canada (CA 🇨🇦), Mexico (MX 🇲🇽) with real content references
- **API routes**: `/api/countries` (GET list + POST create), `/api/countries/[slug]` (GET single + PUT update) — auth required for mutations
- **Public pages**: `/[countrySlug]` dynamic route with `VALID_SLUGS` whitelist (['colombia', 'canada', 'mexico']). Server component resolves content paths → DB records. Client component renders full editorial structure matching homepage pattern:
  - Magazine Hero (generous padding, prominent card with shadow, large title sizing)
  - Top Banner → News (Newspaper icon) → Pro Players (Trophy icon) → Mid Banner → Enthusiasts (Heart icon) → Juniors (Users icon) → Tips (Lightbulb icon) → Bottom Banner → Social Media → Community CTA → Newsletter → Footer → Sticky Banner
  - Section headers use `w-10 h-10 rounded-lg bg-brand-neon/10` icon boxes (matching homepage style)
  - Uses `<main className="flex-1">` wrapper with Footer outside (mirrors homepage layout)
  - Sticky banner uses proper `StickyBanner` component from `@/app/_components/sticky-banner` with spacer div
- **IMPORTANT**: When adding new countries, must update `VALID_SLUGS` array in `app/[countrySlug]/page.tsx`
- **Admin**: `/admin/countries` (list with flags), `/admin/countries/[slug]` (edit with 5 tabs: Magazine, Content Boxes, Banners, Social Media, SEO). Added "Countries" with Globe icon in admin sidebar
- **Header nav**: LATAM removed from NAV_ITEMS. Replaced with "Dink Authority World" dropdown (desktop: button with chevron + dropdown list; mobile: separate section with country links). Countries array: Colombia 🇨🇴, Canada 🇨🇦, Mexico 🇲🇽
- **i18n keys**: `country.ctaTagline`, `country.ctaCorrespondent`, `country.ctaCorrespondentDesc`, `country.ctaLearnMore`
- **Content resolution**: Server component parses paths like `/articles/slug` → fetches from Article/Tip/Junior tables, normalizes to uniform { title, imageUrl, excerpt, authorName, publishedAt, type, path } objects

## Sponsor Banner Carousel System
- **Model**: `SponsorBanner` with fields: id, sponsorName, imageUrl, link, isActive, sortOrder, countries (JSON string), sections (JSON string, default `["news"]`)
- **API**: `/api/sponsors` (GET with `?country=X&section=Y` dual filter, `?all=true` for admin, POST) + `/api/sponsors/[id]` (PUT, DELETE)
- **Component**: `app/_components/sponsor-banner-carousel.tsx` — uses embla-carousel-react + embla-carousel-autoplay, props: `className`, `section`
- **Country detection**: Auto-detects from URL path (`/colombia/...` → colombia, else `central`)
- **Admin**: `/admin/sponsors` — full CRUD with dual checkbox groups: Región (Dink Central, Colombia, México, Canada) + Sección (News, Pro Players, Enthusiasts, Juniors, Tips, Results, Events, Gear, Magazine, Shop), image preview, active toggle, sort order
- **Integration points**:
  - Articles page: `section={category || 'news'}` — dynamically filters by active category tab
  - Tips page: `section="tips"` — between hero and content grid
  - Juniors page: `section="juniors"` — between hero and content grid
  - Magazine page: `section="magazine"` — after first 4 edition cards
  - Shop page: `section="shop"` — after 4th product (ProductCard extracted as local component, grid split into first 4 + carousel + remaining)
- **Filtering logic**: Prisma `contains` on JSON strings (e.g., `sections: { contains: "news" }`)
- **Exclusivity**: Natural via config — activate only desired banners per region+section combo
- **Country pages integration**: `/[countrySlug]` pages have `SponsorBannerCarousel` in each `ContentSection` via `sponsorSection` prop: News→"news", Pro Players→"pro-players", Enthusiasts→"enthusiasts", Juniors→"juniors", Tips→"tips"
- **Auto country detection**: Carousel auto-detects country from URL path, so `/colombia` pages automatically filter by "colombia" region
- **Future countries**: Any new `/[countrySlug]` page automatically gets sponsor banners — just add sponsors with that country in admin
- **Seeded sponsors (10 placeholders, all regions)**:
  - Sponsor News → all regions / news (coca-cola-banner.jpg)
  - Sponsor Pro Players → all regions / pro-players (hilton-banner.jpg)
  - Sponsor Enthusiasts → all regions / enthusiasts (gearbox-banner.jpg)
  - Sponsor Juniors → all regions / juniors (pickleball-central-banner.jpg)
  - Sponsor Tips → all regions / tips (joola-banner.jpg)
  - Sponsor Results → all regions / results (coca-cola-banner.jpg)
  - Sponsor Events → all regions / events (hilton-banner.jpg)
  - Sponsor Places → all regions / places (gearbox-banner.jpg)
  - Sponsor Magazine → all regions / magazine (pickleball-central-banner.jpg)
  - Sponsor Shop → all regions / shop (joola-banner.jpg)

## GEAR → PLACES Rename
- **GEAR section renamed to PLACES** across the entire site (nav, translations, sponsors, admin, SEO)
- **Route**: `/places` → redirects server-side to `/articles?category=places`
- **SEO**: Title "Pickleball Places | Courts, Clubs and Resorts", meta description about courts, clubs, resorts, destinations
- **Heading**: "Pickleball Places" with subtitle when viewing places category on articles page
- **i18n keys**: `nav.places`, `category.places` (EN: Places/Pickleball Places, ES: Lugares/Lugares de Pickleball, PT: Lugares/Lugares de Pickleball)
- **Legacy**: `gear` category still exists in admin article form (`article-form-client.tsx`) for backward compatibility with existing articles
- **Admin sponsors**: Section dropdown uses `places` (not `gear`)
- **Seed**: SPONSOR_SECTIONS uses `places`
- **DB**: 5 sponsor banners updated from section "gear" to "places"
- **Section values**: homepage, news, pro-players, enthusiasts, juniors, tips, results, events, places, magazine, shop
