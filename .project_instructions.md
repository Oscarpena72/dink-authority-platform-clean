## SEO Editorial Control System (Added May 2026)

### New Database Fields on Article Model
- `ogTitle String?` — Open Graph title override for social sharing
- `ogDescription String?` — Open Graph description override for social sharing  
- `noindex Boolean @default(false)` — Hide from search engines
- `focusKeyword String?` — Primary keyword for SEO tracking
- `secondaryKeywords String?` — JSON string array of secondary keywords

### Existing Fields Repurposed
- `metaTitle` → Now serves as "SEO Title" in the editor (independent from editorial title)
- `metaDescription` → Now serves as "SEO Description" in the editor (independent from excerpt)

### Component Architecture
- `seo-search-control.tsx` — Standalone SEO & Search Control panel component
- Replaces the old basic collapsible SEO section in `article-form-client.tsx`
- AI-powered "Generate SEO Suggestions" button via `/api/seo-suggest` endpoint (uses LLM API)

### Metadata Cascade (in buildArticleMetadata)
- OG Title: `ogTitle` → `metaTitle` → `title`
- OG Description: `ogDescription` → `metaDescription` → `excerpt` → auto-generated
- Noindex: respects `article.noindex` boolean in robots meta

### Slug Handling
- POST: Accepts custom slug, falls back to auto-generated with timestamp suffix
- PUT: Allows slug editing (with sanitization). Warning shown for published articles.
- Uniqueness enforced at DB level and with runtime check

## Bases de datos (CRÍTICO)
- Existen DOS bases separadas:
  - PRODUCCIÓN (sitio en vivo dinkauthoritymagazine.com): base Neon, controlada por el usuario vía su cuenta Neon + variables de entorno de su despliegue Vercel. Tiene 187 artículos (185 publicados, 2 borradores) a jul-2026. NO tiene aún las columnas locale/translationOf.
  - PREVIEW/DEV (este entorno Abacus): base hospedada por Abacus (DATABASE_URL en .env). Se sincronizó copiando los 187 artículos reales de producción (jul-2026) + 1 artículo real extra "Megan Fudge" que solo existe en preview (el usuario confirmó que es real y NO debe borrarse). Sí tiene las columnas locale/translationOf.
- SEGURIDAD: la contraseña de la base de producción (Neon) es un secreto. NUNCA imprimirla en texto plano en respuestas, logs ni archivos. El usuario pidió rotarla. La rotación la hace el usuario en el panel de Neon y luego actualiza la cadena de conexión en las variables de entorno de su Vercel; rotarla NO afecta el entorno de preview (usan bases distintas).

## Validación Entrega 2 (completada en preview, jul-2026)
- Flujo de traducción validado end-to-end con login admin real: endpoint POST /api/articles/[id]/translate crea fila nueva locale-específica, idempotente (1 por idioma), status draft, sin flags hero/featured, translationOf apunta al original EN. PUT preserva locale/translationOf. Se renderiza en /es/tips (listado + detalle) y NO aparece en /tips inglés.
- Artículo de demostración traducido y publicado en preview: "kitchen-line-mastery-reset-game-target-selection-pickleball-tips-magazine-es" (ES). Es un demo en preview; se puede despublicar si se desea.

## Entrega 3 (COMPLETADA en preview, checkpoint "Entrega 3: menu idioma, redirects 301, hreflang y sitemap", SIN deploy a producción)
- **Bloque 5a menú por idioma:** ya estaba hecho en Entrega 1 (`app/_components/header.tsx` LANGUAGE_LINKS EN/ES/PT). Verificado desktop+móvil. Las constantes COUNTRY_SLUGS en header quedan como código muerto inofensivo (el redirect intercepta antes).
- **Bloque 5b redirects 301:** en `middleware.ts` — `COUNTRY_REDIRECT_SLUGS = ['colombia','mexico','canada']`; el primer segmento del path que coincida (incl. subrutas) hace `NextResponse.redirect('/es', 301)`. Matcher ampliado con `/colombia(/:path*)`, `/mexico(...)`, `/canada(...)`. Verificado: los 4 casos devuelven HTTP 301 → /es.
- **Bloque 6a hreflang:** nuevo helper `getHreflangLanguages(article, siteUrl)` en `lib/article-page-data.ts` — resuelve la familia (baseId = translationOf ?? id, busca published con OR id/translationOf), devuelve `{en,es,pt,x-default}` o undefined si no hay hermanos. `buildArticleMetadata` acepta 5º arg `languageAlternates` → `alternates.languages`. Llamado desde los 3 detalle EN (`app/{news,players,tips}/[slug]/page.tsx`, ahora seleccionan id/locale/translationOf) y desde `lib/localized-detail.tsx` (ES/PT). Verificado: pares traducidos emiten hreflang recíproco; artículos sin traducción emiten 0 alternates.
- **Bloque 6b sitemap localizado:** `app/sitemap.ts` ya no filtra locale:'en' — incluye todos los published con `getLocaleArticlePath(slug,category,locale)` + landing/secciones `/es` `/es/{news,players,tips}` y `/pt/...`. `news-sitemap.xml` se dejó EN-only a propósito (Google News). Verificado en sitemap.xml.
- Las 3 Entregas (1,2,3) están completas y verificadas SOLO en preview. Falta aprobación final del usuario para deploy a producción, que además requiere migración aditiva de columnas locale/translationOf en la Neon DB de prod (con aprobación explícita).

## DESPLIEGUE A PRODUCCIÓN (COMPLETADO — 14 jul 2026)
- Migración aditiva en Neon prod (branch `production`, db `neondb`) ejecutada manualmente por el usuario vía Neon SQL Editor: agregó columnas `locale` (TEXT NOT NULL DEFAULT 'en') y `translationOf` (TEXT) + 2 índices a la tabla Article. Confirmado: 187/187 artículos marcados locale='en', sin pérdida de datos.
- Código de las 3 Entregas publicado a GitHub (`Oscarpena72/dink-authority-platform-clean`) en ramas `master` y `main` (commit 94890fe). Vercel despliega automáticamente desde GitHub. El sitio vivo es dinkauthoritymagazine.com.
- IMPORTANTE: el push de git NO se puede hacer con `git remote set-url` (bloqueado por el sistema de checkpoint). Se usó `git push https://user:token@github.com/... master:master` y `master:main` pasando el token inline.
- El token de GitHub anterior fue rotado por el usuario; se usó uno nuevo (nunca exponerlo en salida).

## Home localizada /es y /pt (edición digital + estructura completa)
- `/es` y `/pt` ahora replican la estructura de la home EN, localizada.
- Data helper: `lib/locale-home-data.ts` → `getLocaleHomeData(locale)`. Lee filas de `MagazineEdition` y filtra las cuyo campo JSON `countries` incluye el locale ('es'/'pt'). `currentEdition` = la marcada en `currentFor` (o la más reciente del locale); `pastEditions` = resto (máx 3). También trae featured articles (locale), events y results globales, y siteSettings.
- Componente: `app/_components/locale-home.tsx` (client). Renderiza header, portada de edición actual clickable a `/magazine/[slug]`, featured stories, `UpcomingEvents`, `RecentResults`, ediciones previas, newsletter, footer.
- `app/es/page.tsx` y `app/pt/page.tsx` son async y renderizan `<LocaleHome>`.
- `UpcomingEvents` y `RecentResults` aceptan prop opcional `locale?: Locale`: cuando se pasa, título/subtítulo se localizan via `translate()`; sin ella, quedan en inglés (home EN intacta).
- Admin: `BUILT_IN_COUNTRIES` en `admin-magazine-client.tsx` incluye `es` (🇪🇸 Español /es) y `pt` (🇧🇷 Português /pt). Oscar sube portada+PDF y marca es/pt en "Show on these sites" y "Current Issue For", igual que EN. Sin precarga de ediciones.
- Sin migración de schema: solo LEE campos existentes (MagazineEdition.countries/currentFor, Article.locale).
